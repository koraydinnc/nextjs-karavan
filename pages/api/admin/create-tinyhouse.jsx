export default async function handler(req,res) {

    if (req.method === 'POST') {
         try {
             const {price, name, description, city, district,photos} = req.body
         } catch (error) {
            
         }
    }
    
}